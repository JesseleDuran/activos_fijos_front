import React, { Component } from "react";
import styled from "styled-components";

const EllipsableText = styled.p`
  margin: 0;
  overflow:hidden;
  text-overflow: ellipsis;
`;

export default EllipsableText;
